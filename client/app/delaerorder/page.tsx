import Page from "@/components/ProductViewPage";
import { Suspense } from "react";

const DealerOrder = () => {
  return (
    <Suspense>
      <Page title="Order" />
    </Suspense>
  );
};

export default DealerOrder;
